# 健診・人間ドック 予約依頼フォーム

長崎北徳洲会病院 健康管理センターの、Web予約依頼受付システム。

## このプロジェクトの目的

健診・人間ドックの予約受付が電話のみで、**受付スタッフの電話対応負荷が高い**。これを減らすことが目的であり、Web化そのものが目的ではない。

判断に迷ったときの基準は「**受付スタッフの電話が1本減るか、1本あたりが短くなるか**」。ここに効かない機能は作らない。

## 担当者

- 栗原剛（情報システム・総務）。院内SEは1名体制
- 実装・保守は本人が行う。**引き継ぎ相手はいない前提で、後から自分が読んで直せるコードにする**
- 公式サイト（www.nk-toku.jp）の更新は外部制作業者が実施。自分では触れない

---

## 方式の決定（重要）

サイト更新に業者が挟まるため、**フォーム本体をサイト側に置かない**。

```
[公式サイト /medical_checkup/reservation.php]
        │  業者作業はここだけ（リンクを1本追加）
        ▼
[GAS Webアプリ] 予約依頼フォーム（自作・自分で保守）
        │
        ├─→ Googleスプレッドシート「予約依頼台帳」へ直接1行追加
        ├─→ 受付担当へ通知メール
        └─→ 申込者へ自動返信メール
```

### なぜこの方式か

| | 案B：サイト内にPHPフォーム | **案D：GAS Webアプリ（採用）** |
|---|---|---|
| 業者作業 | フォーム3画面＋メール送信＋検証 | **リンク1本の追加のみ** |
| 項目を1つ追加したい | 見積→発注→実装。数週間 | **自分で15分** |
| コース名・料金の改定 | 業者作業または設定ファイル経由 | **自分でコード修正** |
| 台帳への書き込み | メール本文をパース（壊れやすい） | **スプレッドシートへ直接書く。パース不要** |
| URL | `www.nk-toku.jp/...`（病院ドメイン） | `script.google.com/...`（**病院ドメインにできない**） |
| 費用 | 初期・改修のたびに発生 | ほぼゼロ |
| 前提 | なし | Googleクラウド利用の院内承認が必要 → **2026-09-16 承認取得済み** |

案Dの唯一の弱点はURLが病院ドメインにならないこと。リンク元のページで「病院が用意した申込フォームです」と明示し、フォーム側も病院サイトと同系統のデザインにして補う。

**2026-09-16、Googleクラウド利用の院内承認を取得した。案Dで確定。** 案Bへの切り戻しは、これで想定される事態ではなくなった。

ただし業者向け仕様（`docs/02_メール仕様.md` と `vendor/`）は**削除せずそのまま残す**。将来サイト内へフォームを移す判断をした場合にそのまま発注仕様として使えるため。

---

## 現在の状況

- 要件定義・メール仕様・受付可能日カレンダー仕様・サイト調査は完了（`docs/`）
- **GAS側はコードが揃った**（`gas/`）。画面・送信処理・メール2通・定期通知まで実装済み。
  手元での検証は通っている（バックエンド65項目／画面46項目）が、**まだデプロイしていない**
- `mock/index.html` は**業者提示用の初期モック**。コース一覧が古いので参照しないこと。
  本番の画面は `gas/index.html`（公式サイトの実物に合わせ、胃の検査をオプションに一本化済み）

## 次にやること

```
1. スプレッドシート2シート（予約依頼台帳／受付可能日）を作る
2. config.gs の SPREADSHEET_ID とメールアドレスを差し替える
3. setupLedgerSheet() / setupTriggers() を実行、onEditAvailability を手で登録
4. ウェブアプリとしてデプロイ
5. ダミー10件で疎通確認（メール到達・文字化け・二重起票）
6. 受付担当2〜3名に触ってもらう
7. 業者へリンク追加を依頼（docs/06_サイトへの導線設置.md）
```

手順の詳細とトラブル対処は `gas/README.md` にある。

**2026-09-25、Workspace の契約が完了した。承認条件は2つとも満たしている。**
本番運用を止めていた条件は無くなり、あとは上の1〜7を進めるだけ。

6 は `docs/kensin-form-screens.html`（画面サンプル）でデプロイ前に前倒しできる。
これは全18画面のスクリーンショットを並べた資料で、受付担当に文言を見てもらうために作った。
実装を直したら `node tools/render-screens.js` → `node tools/shoot-screens.js` で撮り直す。

---

## 実装の方針

- **依存を増やさない。** 外部ライブラリ・フレームワークを使わない。素のHTML／CSS／JavaScriptとGASのみ
- **1ファイルにまとめすぎない。** GASは `main.gs`（エントリ）／`form.gs`（送信処理）／`sheet.gs`（台帳）／`mail.gs`（メール）／`config.gs`（コース定義など）に分ける
- **コース名・料金・オプション・受付時間などの変わる値は `config.gs` に集約する。** コードの中に直書きしない
- **検証はサーバ側（GAS）で必ず行う。** 画面側のチェックは親切機能であって、防御ではない
- コメントは日本語で書く。半年後の自分が読む

## やらないこと

- リアルタイムの空き枠表示、Web上での予約確定、決済
- 電子カルテ・健診システムとの連携
- 事業所健診の申し込み（別導線）
- ログイン・マイページ
- 添付ファイルの受け取り

---

## 個人情報の扱い（厳守）

### 2026-09-16 の承認に付いた条件（守らないと承認が無効になる）

- **Google Workspace（有償・独自ドメイン）の契約が前提。** 無償Gmailアカウントでの運用は不可。
  **2026-09-25 に8名＋情シス分の契約が完了した。**条件は満たしているが、前提が消えたわけではない。
  無償アカウントへ戻した時点で承認が無効になる
- **台帳データの保持期間は1年まで。** これは上限であって目安ではない。放置すると条件違反になるため、年次でCSVへ退避したうえで台帳から削除する仕組みを必ず入れる（`notify.gs` の時間主導トリガー）

`docs/01_要件定義.md` にある「月1回CSVエクスポート」はバックアップの話であって、削除の話ではない。**退避と削除は別物。**

### 取得と保管

- フォームで取得する健康関連の項目は「**該当あり／該当なし／わからない**」の3択のみ。病名・薬剤名・症状は取得しない
- 申込者への自動返信に、安全確認の回答・保険証番号・生年月日を**含めない**
- 台帳の共有は特定ユーザー限定。「リンクを知る全員」は使わない（`docs/05_台帳の限定共有.md`）
- ソースコードや設定に、実在の受診者データを含めない

---

## ファイル構成

```
（リポジトリ直下）
├ CLAUDE.md                        このファイル
├ docs/
│  ├ 01_要件定義.md                §7とF-06は案B前提のため無効。CLAUDE.mdが優先
│  ├ 02_メール仕様.md              案Bに切り替えた場合の業者向け仕様も兼ねる
│  ├ 03_受付可能日カレンダー仕様.md
│  ├ 04_サイト調査メモ.md          2026-09-04時点の公式サイト調査結果
│  ├ 05_台帳の限定共有.md          Googleスプレッドシートの限定公開方式
│  ├ 06_サイトへの導線設置.md      業者へのリンク追加依頼
│  ├ kensin-form-design.html       関係者共有用の設計案ページ（docs/の要約。正本ではない）
│  ├ kensin-form-screens.html      受付担当に見てもらう画面サンプル（全18画面）
│  └ screens/                      上で使うスクリーンショット。shoot-screens.js が生成する
├ mock/
│  └ index.html                    業者提示用の初期モック。コース一覧が古い。参照しないこと
├ gas/                             本体。セットアップ手順は gas/README.md
│  ├ config.gs                     変わる値の集約。改定はここだけ直す
│  ├ main.gs                       doGet（画面配信）／doPost（送信受付）
│  ├ form.gs                       サーバ側の入力検証
│  ├ sheet.gs                      台帳への書き込み、受付可能日の読み込み
│  ├ mail.gs                       メール2通
│  ├ notify.gs                     枯渇・未更新の通知、保持期間の退避と削除
│  └ index.html                    申込画面（JavaScript 無効でも送信できる）
├ tools/                           手元の確認用。GASへは上げない
│  ├ gas-sandbox.js                GASのAPIを差し替えて .gs を手元で動かす土台
│  ├ local-test.js                 doPost を手元で通す（node tools/local-test.js）
│  ├ render-form.js                index.html を普通のHTMLに書き出す
│  ├ render-screens.js             全画面を preview-screens/ に書き出す
│  └ shoot-screens.js              その画面を撮って docs/screens/ に入れる（playwright が要る）
└ vendor/                          業者へ渡す資料
```

## 未決事項（着手前に決める）

| # | 項目 | 影響 |
|---|---|---|
| ~~1~~ | ~~Googleクラウドに受診者情報を置いてよいか~~ | **決定済み（2026-09-16 承認）。案Dで確定** |
| ~~2~~ | ~~Google Workspace（有償）を使えるか~~ | **決定済み。承認条件として契約が前提。無償アカウント不可** |
| 3 | 正式なコース一覧 | **2026-09-16 に公式サイトから取得して反映済み。**残るのは下記の要確認分のみ |
| 4 | 受付可能日の更新当番と、確保する候補日数 | 運用が回るかどうか |
| 5 | 現在の電話件数（1週間計測） | 効果測定の基準値 |
| ~~6~~ | ~~Workspaceの契約手続き（8名＋情シス分）~~ | **決定済み（2026-09-25 契約完了）。承認条件は2つとも満たした** |

### #3 の残り（健診部門へ要確認）

公式サイト（2026-09-16 取得）に記載が無かったもの。**根拠が確認できないため、フォームの選択肢には出していない。**

| 項目 | 状況 |
|---|---|
| 呼吸器ドック | `docs/04` §5 は about.php を根拠に「コースに追加されている」と書いているが、2026-09-16 時点の about.php・course.php・price.php のいずれにも記載が無い |
| レディースドック | 同上。サイトに記載が無い |
| 腫瘍マーカー／骨密度 | オプション一覧に記載が無い |
| 特定保健指導 | サイトにはあるが健診ではなく指導。このフォームで受け付けるかが未定 |
| 各オプションの絶食要否 | サイトに記載が無い。現行の注意文は推測のため（仮）扱い |
